# Telegram mirror verification

This record covers the bounded issue-5 architecture and is maintained with the real-extension fake-transport regression.

## 2026-08-21

The installed Pi package was inspected against its extension, session-format, and TUI contracts before implementation.
The focused checks are `bash tests/fm-telegram-mirror.test.sh`, `bash tests/fm-telegram-mirror-live-e2e.test.sh`, and `bash tests/fm-telegram.test.sh`.
The live lifecycle regression loads the tracked project extension through the installed Pi SDK runtime with its faux model provider and an executable fake transport, without a Telegram token or real home state.
It proves lock-owned admission, durable session provenance, live origin labels, the generated Telegram authority boundary, structural exclusions, terminal and assistant fan-out, definitive completion, fresh extension reload and session replacement reconciliation, no terminal history replay, and the accepted acceptance-before-persistence anomaly.
The Python transport regression proves mode-off callback refusal with Cancel cleanup, no operational Telegram wake publication, Unicode-safe chunk delivery, bounded rejection retries, delivery-unknown non-retry, bounded paired delivery retention, handled completion, and legacy wake retirement.
